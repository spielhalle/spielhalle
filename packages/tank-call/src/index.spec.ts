/*!
 * Source https://github.com/spielhalle/spielhalle Package: tank-call
 */

import { expect } from 'chai';
import 'mocha';

// tslint:disable:no-unused-expression
describe('index', (): void => {
    it('should contain FlowApiValidator', (): void => {
        expect(2).to.to.exist;
    });
});
