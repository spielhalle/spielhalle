h2(mat-dialog-title) Delete all
mat-dialog-content
    - for (var y = 1; y <=9; y++)
        button(mat-button,[mat-dialog-close]=y)= y
mat-dialog-actions
    button(mat-button,mat-dialog-close) Close
    // The mat-dialog-close directive optionally accepts a value as a result for the dialog.
    button(mat-button,[mat-dialog-close]=0) Clear